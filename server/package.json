{
  "name": "deejii-server",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && export NODE_ENV='production' && node dist/index",
    "dev": "npm run build && export NODE_ENV='development' && npm run watch",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf dist",
    "compile": "tsc -p ./",
    "compile:dev": "tsc -w -p ./",
    "watch:server": "nodemon dist/index",
    "watch": "run-p compile:dev watch:server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vseppala/deejii.git"
  },
  "author": "Ville Seppälä <ville@villeseppala.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/vseppala/deejii/issues"
  },
  "homepage": "https://github.com/vseppala/deejii#readme",
  "devDependencies": {
    "@types/body-parser": "^0.0.33",
    "@types/debug": "^0.0.29",
    "@types/express": "^4.0.34",
    "@types/morgan": "^1.7.32",
    "@types/node": "^6.0.52",
    "nodemon": "^1.11.0",
    "npm-run-all": "^3.1.2",
    "typescript": "^2.1.4"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "debug": "^2.4.5",
    "express": "^4.14.0",
    "morgan": "^1.7.0"
  }
}
